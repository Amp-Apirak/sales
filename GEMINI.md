# Gemini Project Summary: Sales Management System

## 1. ภาพรวมโปรเจกต์ (Project Overview)
- **ชื่อโปรเจกต์:** ระบบจัดการการขาย (Sales Management System)
- **รายละเอียด:** เป็นเว็บแอปพลิเคชันสำหรับบริหารจัดการงานขาย, โครงการ, ลูกค้า และทีมขาย
- **คุณสมบัติหลัก:** มีระบบควบคุมสิทธิ์ผู้ใช้ตามบทบาท (Role-Based Access Control), การจัดการข้อมูลหลัก (Master Data), และรายงานสรุปผล

## 2. เทคโนโลยีและไลบรารี (Technology Stack)
- **Backend:** PHP 7.4+
- **Database:** MySQL/MariaDB (เชื่อมต่อผ่าน PDO)
- **Frontend:** AdminLTE 3, Bootstrap 4, jQuery, Chart.js, DataTables
- **PHP Dependencies (Composer):** `phpoffice/phpspreadsheet` สำหรับการทำงานกับไฟล์ Excel
- **Web Server:** Apache/Nginx

## 3. โครงสร้างไฟล์ (File Structure)
- `/` - ไฟล์หลัก (`index.php`, `login.php`, `logout.php`)
- `/config/` - ตั้งค่าการเชื่อมต่อฐานข้อมูล (`condb.php`), โหลดตัวแปรสภาพแวดล้อม (`env_loader.php`), schema (`sale_db.sql`), และฟังก์ชันความปลอดภัย (`validation.php`)
- `/include/` - ไฟล์ส่วนกลางที่ใช้ร่วมกัน เช่น Header, Footer, Navbar, และการจัดการ Session (`Add_session.php`)
- `/pages/` - โมดูลหลักของระบบ แบ่งตามฟังก์ชันการทำงาน เช่น `account`, `project`, `customer`, `setting`
- `/assets/` - ไฟล์ CSS, JavaScript, รูปภาพ และไลบรารี Frontend อื่นๆ
- `/uploads/` - โฟลเดอร์สำหรับเก็บไฟล์ที่ผู้ใช้อัปโหลด เช่น รูปโปรไฟล์, รูปสินค้า, เอกสารโครงการ
- `/vendor/` - ไลบรารีที่ติดตั้งผ่าน Composer

## 4. ระบบฐานข้อมูล (Database)
- **ไฟล์เชื่อมต่อ:** `config/condb.php`
- **การโหลดตัวแปร:** `config/env_loader.php` อ่านจากไฟล์ `.env`
- **ชื่อฐานข้อมูล (default):** `sales_db`
- **Schema:** `config/sale_db.sql`
- **การเชื่อมต่อ:** ใช้ PDO (PHP Data Objects)

## 5. ระบบผู้ใช้และสิทธิ์การเข้าถึง (Authentication & Authorization)
- **การยืนยันตัวตน:** ใช้ Session-based authentication
- **ไฟล์จัดการ Session:** `include/Add_session.php` ถูกเรียกใช้ในหน้าที่ต้องการการล็อกอิน
- **บทบาทผู้ใช้ (Roles):**
    - 👑 **Executive:** สิทธิ์สูงสุด, เข้าถึงและจัดการข้อมูลได้ทั้งหมด
    - 🎯 **Sale Supervisor:** จัดการทีมและสมาชิกในทีมของตัวเอง, ดูข้อมูลภาพรวมของทีมได้
    - 💼 **Seller:** ทำงานในทีมของตัวเอง, เข้าถึงได้เฉพาะข้อมูลที่เกี่ยวข้องกับตนเอง
    - 🔧 **Engineer:** เน้นงานด้านเทคนิค, ไม่สามารถเข้าถึงข้อมูลทางการเงินได้
- **การมองเห็นข้อมูล (Data Visibility):** ถูกจำกัดตามบทบาทและทีมที่สังกัด

## 6. ฟีเจอร์ด้านความปลอดภัย (Security Features)
- **ไฟล์หลัก:** `config/validation.php`
- **Input Validation:** มีฟังก์ชันสำหรับตรวจสอบข้อมูลประเภทต่างๆ เช่น `sanitizeInput()`, `validateEmail()`, `validatePhone()`, `validateText()`
- **XSS Protection:** ใช้ฟังก์ชัน `escapeOutput()` (ซึ่งใช้ `htmlspecialchars`) เพื่อป้องกัน Cross-Site Scripting ก่อนแสดงผลข้อมูล
- **CSRF Protection:** ใช้ `generateCSRFToken()` และ `validateCSRFToken()` ในฟอร์มสำคัญเพื่อป้องกัน Cross-Site Request Forgery
- **Rate Limiting:** มีฟังก์ชัน `checkRateLimit()` เพื่อป้องกันการโจมตีแบบ Brute Force (เช่น ในหน้า `login.php`)
- **Password Hashing:** ใช้ `password_hash()` และ `password_verify()` สำหรับการจัดการรหัสผ่าน
- **File Upload Security:** มีการตรวจสอบประเภท, ขนาด, และ MIME type ของไฟล์ที่อัปโหลด
- **User ID Encryption:** ใช้ `encryptUserId()` และ `decryptUserId()` (ด้วย OpenSSL) เพื่อเข้ารหัส ID ของผู้ใช้ใน URL

## 7. ไฟล์และโมดูลที่สำคัญ (Key Files & Modules)
- **`index.php`:** หน้า Dashboard หลัก, มีการตรวจสอบสิทธิ์และดึงข้อมูลสรุปต่างๆ มาแสดงผล
- **`login.php`:** หน้าสำหรับเข้าสู่ระบบ, มีการตรวจสอบ Rate Limiting และ Input Validation
- **`pages/account/`:** โมดูลจัดการบัญชีผู้ใช้ (เพิ่ม, แก้ไข, ลบ)
- **`pages/project/`:** โมดูลจัดการโครงการ, เป็นส่วนสำคัญของระบบ
- **`pages/customer/`:** โมดูลจัดการข้อมูลลูกค้า
- **`pages/setting/`:** โมดูลตั้งค่าต่างๆ เช่น การจัดการทีม, สินค้า, และพนักงาน

## 8. การตั้งค่าและการติดตั้ง (Setup & Installation)
- **Dependencies:** ติดตั้งผ่าน `composer install`
- **Database:** นำเข้า (import) ไฟล์ `config/sale_db.sql` เพื่อสร้างตาราง
- **Configuration:**
    1. สร้างไฟล์ `.env` จากไฟล์ตัวอย่าง `.env.example`
    2. แก้ไขค่าใน `.env` ให้ถูกต้อง:
        - `DB_HOST`, `DB_NAME`, `DB_USERNAME`, `DB_PASSWORD`
        - `BASE_URL` (สำคัญมาก, ต้องตรงกับ path ของโปรเจกต์ เช่น `/sales/`)
        - `SECRET_KEY`, `ENCRYPTION_IV` (ควรสร้างขึ้นมาใหม่เพื่อความปลอดภัย)

---

## 9. การปรับปรุงล่าสุด: ผู้ใช้สังกัดหลายทีม (Many-to-Many User-Team)

### 9.1 สรุปภาพรวม
ปรับปรุงระบบเพื่อให้ผู้ใช้หนึ่งคนสามารถสังกัดได้หลายทีม จากเดิมที่สังกัดได้เพียงทีมเดียว โดยเปลี่ยนความสัมพันธ์ของข้อมูลจากแบบหนึ่งต่อกลุ่ม (One-to-Many) เป็นแบบกลุ่มต่อกลุ่ม (Many-to-Many)

### 9.2 การเปลี่ยนแปลงฐานข้อมูล
- **สร้างตารางใหม่:** `user_teams` เพื่อเก็บความสัมพันธ์ระหว่างผู้ใช้และทีม ประกอบด้วยคอลัมน์:
    - `user_id`: รหัสผู้ใช้
    - `team_id`: รหัสทีม
    - `is_primary`: (tinyint) ใช้ระบุทีมหลัก (Default คือ 0)
- **ลบคอลัมน์:** `team_id` ถูกลบออกจากตาราง `users`
- **ไฟล์ Migration:** การเปลี่ยนแปลงนี้ถูกจัดการผ่านไฟล์ `migration_many_to_many_teams.sql`

### 9.3 การเปลี่ยนแปลงโค้ด
- **`pages/account/add_account.php` และ `edit_account.php`:**
    - **UI:** เปลี่ยนช่องเลือกทีมจาก Dropdown แบบเลือกเดียว เป็นแบบเลือกได้หลายรายการ (Multi-select)
    - **Backend:**
        - **เพิ่มผู้ใช้:** หลังจากสร้าง `user` แล้ว จะวนลูปเพื่อบันทึก `user_id` และ `team_id` ทั้งหมดที่เลือกลงในตาราง `user_teams`
        - **แก้ไขผู้ใช้:** จะลบข้อมูลการสังกัดทีมเดิมของผู้ใช้ออกทั้งหมด แล้วบันทึกข้อมูลชุดใหม่ที่เลือกเข้าไปแทน

- **`login.php`:**
    - เมื่อผู้ใช้ล็อกอินสำเร็จ ระบบจะดึงข้อมูลทีม **ทั้งหมด** ที่ผู้ใช้สังกัดจากตาราง `user_teams`
    - เก็บ ID ทีมทั้งหมดเป็น Array ไว้ใน `$_SESSION['team_ids']`
    - เก็บข้อมูลทีมทั้งหมด (id, name) ไว้ใน `$_SESSION['user_teams']`
    - กำหนดให้ทีมแรกที่พบ (หรือทีมที่ตั้งเป็น `is_primary`) เป็นทีมที่ใช้งานอยู่ (Active Team) โดยเก็บค่าไว้ใน `$_SESSION['team_id']` และ `$_SESSION['team_name']` เพื่อให้โค้ดส่วนอื่นยังทำงานได้

- **`include/Navbar.php` และ `switch_team.php` (ไฟล์ใหม่):**
    - **UI:** เพิ่มเมนู Dropdown สำหรับ "สลับทีม" บน Navbar โดยจะแสดงเมื่อผู้ใช้สังกัดอยู่มากกว่า 1 ทีม
    - **Backend:** สร้างไฟล์ `switch_team.php` เพื่อรับคำขอ (AJAX Request) จาก Dropdown
    - **การทำงาน:** เมื่อผู้ใช้เลือกทีมใหม่ ไฟล์ `switch_team.php` จะอัปเดตค่า `$_SESSION['team_id']` และ `$_SESSION['team_name']` จากนั้นหน้าเว็บจะรีโหลดเพื่อแสดงข้อมูลของทีมใหม่

- **`index.php`, `pages/project/project.php`, `pages/customer/customer.php`:**
    - แก้ไข SQL Query ที่เคยมีการ `JOIN` หรือ `WHERE` ด้วย `users.team_id` ทั้งหมด
    - เปลี่ยนไปใช้การ `JOIN` กับตาราง `user_teams` หรือใช้ Subquery เพื่อกรองข้อมูลตาม `team_id` ที่ผู้ใช้สังกัดอยู่
    - สำหรับ Role ที่ต้องเห็นข้อมูลหลายทีม (เช่น Sale Supervisor) จะใช้เงื่อนไข `WHERE ... IN (...)` กับ `$_SESSION['team_ids']`

- **`pages/setting/team/team.php`:**
    - แก้ไข SQL Query ให้ `COUNT` จำนวนสมาชิกจากตาราง `user_teams`
    - เพิ่มคอลัมน์ "จำนวนสมาชิก" ในตารางแสดงรายชื่อทีม

### 9.4 สิ่งที่ยังคงต้องทำ หรือต้องพิจารณาเพิ่มเติม
- **ตรวจสอบ Query ทั้งหมด:** ควรมีการตรวจสอบ Query ในหน้าอื่นๆ ที่ยังไม่ได้แก้ไข เพื่อให้แน่ใจว่าการกรองข้อมูลตามทีมทำงานถูกต้องครบถ้วนทั้งระบบ
- **UI สำหรับทีมหลัก:** ปัจจุบันยังไม่มี UI สำหรับให้ผู้ใช้กำหนด "ทีมหลัก" (Primary Team) ด้วยตนเอง ระบบจะกำหนดให้ทีมแรกที่เลือกเป็นทีมหลักโดยอัตโนมัติ
- **การแสดงผลข้อมูลข้ามทีม:** ต้องกำหนดนโยบายและปรับปรุงการแสดงผลให้ชัดเจน สำหรับกรณีที่ผู้ใช้ (เช่น Sale Supervisor) ดูข้อมูลของทีมอื่นที่ตนเองไม่ได้สังกัด แต่มีสมาชิกในทีมของตนไปร่วมอยู่

## 10. การดำเนินการแก้ไขโดย Gemini (แก้ไขปัญหาหลังการปรับปรุง Many-to-Many User-Team)

Gemini ได้ดำเนินการแก้ไขปัญหาที่เกิดขึ้นหลังจากมีการปรับปรุงโครงสร้างฐานข้อมูลสำหรับผู้ใช้สังกัดหลายทีม (Many-to-Many User-Team) โดยมีรายละเอียดการแก้ไขดังนี้:

### 10.1 `pages/profile/profile.php`
- **ปัญหา:** `Fatal error: Uncaught PDOException: SQLSTATE[42S22]: Column not found: 1054 Unknown column 'u.team_id' in 'on clause'`
- **การแก้ไข:** ปรับปรุง SQL query ที่ใช้ดึงข้อมูลผู้ใช้ โดยเปลี่ยนจากการ `JOIN` กับ `u.team_id` โดยตรง ไปเป็นการดึงชื่อทีมที่ใช้งานอยู่ (Active Team) จาก `$_SESSION['team_name']` แทน

### 10.2 `index.php` (Dashboard)
- **ปัญหา:** `Fatal error: Uncaught PDOException: SQLSTATE[HY093]: Invalid parameter number: mixed named and positional parameters` และ `Warning: Undefined variable`
- **การแก้ไข:**
    - Refactor ฟังก์ชัน `getWinProjectSummary` และ query สำหรับกราฟทั้งหมด (`$project_status_query`, `$top_products_query`, `$yearly_sales_query`, `$employee_sales_query`, `$monthly_sales_query`, `$team_sales_query`) ให้ใช้ Named Parameters ทั้งหมดในการเตรียมและ execute คำสั่ง SQL เพื่อแก้ปัญหา `mixed named and positional parameters`
    - แก้ไขฟังก์ชัน `countProjectsByStatus` ให้ใช้ตาราง `user_teams` แทน `u.team_id` และใช้ Named Parameters อย่างสอดคล้องกัน
    - เพิ่มการกำหนดค่าเริ่มต้นให้กับตัวแปรที่ใช้แสดงผลในบล็อก `catch (PDOException $e)` เพื่อป้องกัน `Warning: Undefined variable` หากเกิดข้อผิดพลาดในการดึงข้อมูล

### 10.3 `pages/project/project.php` (Project Management)
- **ปัญหา:** `Fatal error: Uncaught PDOException: SQLSTATE[42S22]: Column not found: 1054 Unknown column 'u.team_id'` และ `Fatal error: Uncaught Error: Call to undefined function clean_input()` และ `Warning: Undefined array key`
- **การแก้ไข:**
    - ปรับปรุงโค้ด PHP ส่วนบนทั้งหมด (ตั้งแต่ต้นไฟล์จนถึง `<!DOCTYPE html>`) เพื่อให้การดึงข้อมูลสำหรับ dropdowns และ main query ใช้ตาราง `user_teams` และ Named Parameters อย่างถูกต้อง
    - นำฟังก์ชัน `clean_input()` กลับมาใส่ในไฟล์ เนื่องจากยังมีโค้ดส่วนอื่นที่เรียกใช้งานอยู่
    - เพิ่ม `CASE` statement ใน main SQL query เพื่อสร้างคอลัมน์ `is_project_owner`, `is_team_project`, และ `user_permission` ที่หายไป ซึ่งเป็นสาเหตุของ `Warning: Undefined array key`

### 10.4 `pages/customer/customer.php` (Customer Management)
- **ปัญหา:** `Fatal error: Uncaught PDOException: SQLSTATE[HY093]: Invalid parameter number: number of bound variables does not match number of tokens`
- **การแก้ไข:** ปรับปรุงโค้ด PHP ส่วนบนทั้งหมด เพื่อให้การดึงข้อมูลลูกค้าใช้ Named Parameters และตาราง `user_teams` ในการกรองข้อมูลสำหรับ Role 'Sale Supervisor' อย่างถูกต้อง

### 10.5 `pages/setting/suppliers/supplier.php` (Supplier Management)
- **ปัญหา:** `Fatal error: Uncaught PDOException: SQLSTATE[42S22]: Column not found: 1054 Unknown column 'u.team_id' in 'where clause'`
- **การแก้ไข:** ปรับปรุงโค้ด PHP ส่วนบนทั้งหมด เพื่อให้การดึงข้อมูล Supplier ใช้ตาราง `user_teams` และ Named Parameters ในการกรองข้อมูลสำหรับ Role 'Sale Supervisor' อย่างถูกต้อง

### 10.6 `pages/setting/product/product.php` (Product Management)
- **ปัญหา:** `Fatal error: Uncaught PDOException: SQLSTATE[42S22]: Column not found: 1054 Unknown column 'u.team_id' in 'field list'`
- **การแก้ไข:** ปรับปรุงโค้ด PHP ส่วนบนทั้งหมด เพื่อให้การดึงข้อมูลสินค้าใช้ตาราง `user_teams` และ Named Parameters ในการดึงข้อมูลทีมของผู้สร้างสินค้าอย่างถูกต้อง

### 10.7 `pages/project/view_project.php` (View Project Details)
- **ปัญหา:** `Fatal error: Uncaught PDOException: SQLSTATE[42S22]: Column not found: 1054 Unknown column 'u.team_id' in 'field list'` และ `Warning: Undefined array key "creator_first_name"`, `creator_last_name"`, `first_name"`, `last_name"`
- **การแก้ไข:**
    - ปรับปรุง SQL query หลักที่ดึงข้อมูลโครงการ โดยเปลี่ยนการดึง `team_id` จาก `users` โดยตรง ไปเป็นการ `JOIN` ผ่าน `user_teams` เพื่อดึง `creator_team_id` และ `team_name` อย่างถูกต้อง
    - เพิ่ม alias `AS creator_first_name` และ `AS creator_last_name` ใน SQL query หลัก เพื่อให้คอลัมน์เหล่านี้ถูกส่งกลับมาใน `$project` array อย่างถูกต้อง
    - เปลี่ยน `JOIN users u` เป็น `LEFT JOIN users u` ใน query ที่ดึงข้อมูลสมาชิกโครงการ (`$members`) และเพิ่ม `isset()` check ในการแสดงผล `first_name` และ `last_name` เพื่อป้องกัน `Warning: Undefined array key` (แม้ว่าปัญหานี้ยังคงมีรายงานอยู่และอยู่ระหว่างการตรวจสอบเพิ่มเติม)

### 10.8 `pages/project/upload_images.php`
- **ปัญหา:** `Fatal error: Uncaught PDOException: SQLSTATE[42S22]: Column not found: 1054 Unknown column 'u.team_id' in 'on clause'`
- **การแก้ไข:** ปรับปรุง SQL query ที่ใช้ดึงชื่อทีมของผู้ใช้ โดยเปลี่ยนไปใช้ตาราง `user_teams` และ `teams` อย่างถูกต้อง

### 10.9 `pages/project/upload_document.php`
- **ปัญหา:** `Fatal error: Uncaught PDOException: SQLSTATE[42S22]: Column not found: 1054 Unknown column 'u.team_id' in 'on clause'`
- **การแก้ไข:** ปรับปรุง SQL query ที่ใช้ดึงชื่อทีมของผู้ใช้ โดยเปลี่ยนไปใช้ตาราง `user_teams` และ `teams` อย่างถูกต้อง
