# Employee Document Management - Testing Guide

**วันที่:** 2025-10-12
**เวอร์ชัน:** 1.0

---

## 🧪 การทดสอบระบบ

### Pre-requisites

1. **ตรวจสอบฐานข้อมูล:**
```sql
-- ตรวจสอบว่าตารางสร้างเรียบร้อย
SHOW TABLES LIKE 'employee_%';

-- ควรเห็น:
-- employee_documents
-- employee_document_links

-- ตรวจสอบโครงสร้างตาราง
DESCRIBE employee_documents;
DESCRIBE employee_document_links;
```

2. **ตรวจสอบโฟลเดอร์:**
```bash
ls -la /mnt/c/xampp/htdocs/sales/uploads/employee_documents/
# ควรมี permission 755
```

3. **ตรวจสอบ PHP settings:**
```bash
# ใน php.ini ต้องมี:
upload_max_filesize = 20M
post_max_size = 25M
max_execution_time = 300
```

---

## 🎭 Test Cases แยกตาม Role

### A. Executive Role (Full Access)

#### Test 1: เข้าถึง Tabs
- [ ] Login ด้วย Executive account
- [ ] ไปที่ view_employees.php?id={employee_id}
- [ ] **Expected:** เห็น 3 tabs (ข้อมูลทั่วไป, เอกสารแนบ, ลิงก์เอกสาร)
- [ ] คลิกแต่ละ tab ต้องทำงาน

#### Test 2: อัปโหลดเอกสาร
- [ ] คลิก tab "เอกสารแนบ"
- [ ] คลิกปุ่ม "อัปโหลดเอกสาร"
- [ ] เลือกหมวดหมู่: "เรซูเม่"
- [ ] ใส่ชื่อเอกสาร: "Resume_2024"
- [ ] เลือกไฟล์ PDF ขนาด < 10MB
- [ ] **Expected:** อัปโหลดสำเร็จ, แสดง progress bar, แสดง success alert, reload table

#### Test 3: ดาวน์โหลดเอกสาร
- [ ] คลิกปุ่ม "ดาวน์โหลด" บนเอกสารที่อัปโหลด
- [ ] **Expected:** ดาวน์โหลดไฟล์ได้, ชื่อไฟล์ถูกต้อง

#### Test 4: ลบเอกสาร
- [ ] คลิกปุ่ม "ลบ" บนเอกสาร
- [ ] **Expected:** แสดง SweetAlert2 confirm dialog
- [ ] คลิก "ใช่, ลบเลย!"
- [ ] **Expected:** ลบสำเร็จ, ไฟล์หายจาก DataTable

#### Test 5: เพิ่มลิงก์
- [ ] คลิก tab "ลิงก์เอกสาร"
- [ ] คลิกปุ่ม "เพิ่มลิงก์เอกสาร"
- [ ] เลือกหมวดหมู่: "Google Drive"
- [ ] ใส่ชื่อ: "CV Folder"
- [ ] ใส่ URL: "https://drive.google.com/example"
- [ ] **Expected:** บันทึกสำเร็จ, แสดง success alert, reload table

#### Test 6: แก้ไขลิงก์
- [ ] คลิกปุ่ม "แก้ไข" บนลิงก์
- [ ] **Expected:** Modal เปิด, ข้อมูลเดิมแสดงในฟอร์ม
- [ ] แก้ไขชื่อเป็น "CV Folder Updated"
- [ ] คลิก "บันทึก"
- [ ] **Expected:** แก้ไขสำเร็จ, ชื่อใหม่แสดงใน table

#### Test 7: ลบลิงก์
- [ ] คลิกปุ่ม "ลบ" บนลิงก์
- [ ] **Expected:** แสดง SweetAlert2 confirm
- [ ] คลิก "ใช่, ลบเลย!"
- [ ] **Expected:** ลบสำเร็จ

---

### B. Sale Supervisor Role (Team-based Access)

#### Test 8: เข้าถึง Tabs (พนักงานในทีม)
- [ ] Login ด้วย Sale Supervisor account
- [ ] Switch team หรือเลือก "ALL"
- [ ] ไปที่ view_employees.php?id={employee_in_team}
- [ ] **Expected:** เห็น 3 tabs (เหมือน Executive)
- [ ] สามารถอัปโหลด/ลบเอกสารได้

#### Test 9: ไม่เข้าถึง (พนักงานนอกทีม)
- [ ] Login ด้วย Sale Supervisor account
- [ ] Switch team เป็นทีมเฉพาะ
- [ ] ไปที่ view_employees.php?id={employee_other_team}
- [ ] **Expected:** เห็นเฉพาะ tab "ข้อมูลทั่วไป" (ไม่เห็น 2 tabs เอกสาร)

---

### C. Seller/Engineer Role (No Access)

#### Test 10: ไม่แสดง Tabs
- [ ] Login ด้วย Seller หรือ Engineer account
- [ ] ไปที่ view_employees.php?id={any_employee}
- [ ] **Expected:** เห็นเฉพาะ tab "ข้อมูลทั่วไป" (ไม่เห็นเอกสารและลิงก์)
- [ ] ไม่มีปุ่มอัปโหลดหรือเพิ่มลิงก์

---

## 🔒 Security Test Cases

### Test 11: File Type Validation
- [ ] พยายามอัปโหลดไฟล์ .exe
- [ ] **Expected:** แสดง error "ประเภทไฟล์ไม่ถูกต้อง"
- [ ] ไฟล์ไม่ถูกบันทึก

### Test 12: File Size Validation
- [ ] พยายามอัปโหลดไฟล์ขนาด > 20MB
- [ ] **Expected:** แสดง error "ไฟล์ใหญ่เกินไป"
- [ ] ไฟล์ไม่ถูกบันทึก

### Test 13: URL Validation (HTTPS Only)
- [ ] พยายามเพิ่มลิงก์ด้วย URL: "http://example.com"
- [ ] **Expected:** แสดง error "URL ต้องเริ่มด้วย https://"
- [ ] ลิงก์ไม่ถูกบันทึก

### Test 14: CSRF Token
- [ ] เปิด Developer Tools → Network
- [ ] อัปโหลดเอกสาร
- [ ] ตรวจสอบ POST request
- [ ] **Expected:** มี csrf_token ใน form data

### Test 15: SQL Injection (Manual Test)
- [ ] ใส่ `' OR '1'='1` ในช่องค้นหา DataTable
- [ ] **Expected:** ไม่เกิด error, แสดงผลปกติ (PDO prepared statement ป้องกัน)

---

## 🎨 UI/UX Test Cases

### Test 16: DataTable Functionality
- [ ] คลิกหัวคอลัมน์ (Sort ascending/descending)
- [ ] **Expected:** ข้อมูลเรียงลำดับถูกต้อง
- [ ] ค้นหาด้วยคำว่า "Resume"
- [ ] **Expected:** แสดงเฉพาะรายการที่ตรง
- [ ] เปลี่ยนจำนวนรายการต่อหน้า (10, 25, 50)
- [ ] **Expected:** แสดงตามที่เลือก

### Test 17: Progress Bar
- [ ] อัปโหลดไฟล์ขนาดใหญ่ (10-20MB)
- [ ] **Expected:** เห็น progress bar แสดงความคืบหน้า 0% → 100%

### Test 18: Responsive Design
- [ ] เปิดหน้าด้วย Desktop (1920x1080)
- [ ] **Expected:** แสดงผลสมบูรณ์
- [ ] เปิดหน้าด้วย Tablet (768px)
- [ ] **Expected:** DataTable responsive, ไม่มี horizontal scroll
- [ ] เปิดหน้าด้วย Mobile (375px)
- [ ] **Expected:** Nav pills เปลี่ยนเป็น dropdown

### Test 19: Modal Behavior
- [ ] เปิด Modal อัปโหลด
- [ ] ปิดโดยคลิก "ยกเลิก"
- [ ] **Expected:** ฟอร์มถูก reset, ไฟล์ label เปลี่ยนเป็น "เลือกไฟล์..."
- [ ] เปิด Modal แก้ไขลิงก์
- [ ] ปิดโดยคลิก X
- [ ] เปิดใหม่อีกครั้ง
- [ ] **Expected:** แสดงฟอร์มเปล่า (ไม่ติดค่าเดิม)

---

## 📊 Database Integrity Tests

### Test 20: Foreign Key Constraints
```sql
-- ลองลบพนักงานที่มีเอกสาร
DELETE FROM employees WHERE id = '{employee_with_documents}';

-- ตรวจสอบเอกสาร
SELECT * FROM employee_documents WHERE employee_id = '{employee_with_documents}';

-- Expected: เอกสารถูกลบด้วย (CASCADE)
```

### Test 21: File Path Integrity
```sql
-- ตรวจสอบว่า file_path ถูกบันทึกถูกต้อง
SELECT document_id, file_path FROM employee_documents;

-- Expected: path ต้องเป็น: uploads/employee_documents/{employee_id}/{filename}
```

---

## 🐛 Common Issues & Fixes

### Issue 1: อัปโหลดไม่ได้
**Symptoms:** คลิกอัปโหลดแล้วไม่มีอะไรเกิดขึ้น
**Check:**
1. Console สำหรับ JavaScript errors
2. Network tab สำหรับ 500 error
3. PHP error log: `/xampp/apache/logs/error.log`
4. Permission โฟลเดอร์: `chmod 755 uploads/employee_documents/`

### Issue 2: DataTable ไม่แสดงข้อมูล
**Symptoms:** Table ว่างเปล่าแม้มีข้อมูลในฐานข้อมูล
**Check:**
1. Console สำหรับ AJAX errors
2. ตรวจสอบ `employee_id` ถูกส่งไปถูกต้อง
3. ทดสอบ API โดยตรง: `get_documents.php?employee_id={id}`

### Issue 3: Modal ไม่เปิด
**Symptoms:** คลิกปุ่มแล้ว Modal ไม่ขึ้น
**Check:**
1. ตรวจสอบว่า include modal files ถูกต้อง
2. ตรวจสอบ `$canAccessDocuments` เป็น `true`
3. Console สำหรับ JavaScript errors

---

## ✅ Testing Checklist Summary

### Critical Tests (Must Pass)
- [ ] Executive เห็น 3 tabs
- [ ] Sale Supervisor เห็น tabs เฉพาะทีมตัวเอง
- [ ] Seller/Engineer ไม่เห็น tabs เอกสาร
- [ ] อัปโหลดไฟล์ PDF สำเร็จ
- [ ] ดาวน์โหลดไฟล์สำเร็จ
- [ ] ลบเอกสารสำเร็จ
- [ ] เพิ่มลิงก์ HTTPS สำเร็จ
- [ ] แก้ไขลิงก์สำเร็จ
- [ ] ลบลิงก์สำเร็จ
- [ ] Reject ไฟล์ .exe
- [ ] Reject ไฟล์ > 20MB
- [ ] Reject URL http://

### Nice-to-Have Tests
- [ ] Progress bar แสดงถูกต้อง
- [ ] DataTable sorting/filtering ทำงาน
- [ ] Responsive ทุก screen size
- [ ] Modal reset หลังปิด
- [ ] Thai language ใน DataTable

---

## 🚀 Production Deployment Checklist

- [ ] Backup ฐานข้อมูล
- [ ] รัน SQL script บน production DB
- [ ] Upload โค้ดทั้งหมด
- [ ] ตั้ง permission โฟลเดอร์ `uploads/employee_documents/` = 755
- [ ] ตรวจสอบ php.ini settings (upload_max_filesize, post_max_size)
- [ ] Restart Apache/Nginx
- [ ] ทดสอบ Critical Tests ทั้งหมด
- [ ] แจ้ง users ให้ทดสอบ
- [ ] Monitor error logs สัปดาห์แรก

---

**หมายเหตุ:** หากพบ bug ใดๆ ให้บันทึกลงใน GitHub Issues หรือ bug tracking system พร้อม:
1. Steps to reproduce
2. Expected vs Actual behavior
3. Screenshot/Error message
4. Browser & OS version
5. User role ที่ทดสอบ
